name: news_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  get: ^4.6.6
  get_it: ^7.7.0
  connectivity_plus: ^6.0.3
  cached_network_image: ^3.3.1

  news_core:
    path: packages/news_core
  local_storage_core:
    path: packages/local_storage_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  test: ^1.24.9
  mockito: ^5.4.4
flutter:
  uses-material-design: true
